# Maintenance Protocol

This skill is maintained through SkillPort.

When changing a local agent skill:

- Shared workflow, checklist, decision rule, or validation rule: update `sop.md`.
- Agent-specific tool name, command, install path, or runtime behavior: update the local agent skill or `agents/<agent>.md`.
- If unsure, classify the change before editing.